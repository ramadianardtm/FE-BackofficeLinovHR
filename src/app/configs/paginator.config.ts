/**
 * Constant for action type used in Every Component
 */
export const Paginator = {
    rowsPerPage: 10,
    // rowsPerPageOptions: [10, 20, 50, { showAll: 'All' }],
    rowsPerPageOptions: [10, 20, 50],

};
