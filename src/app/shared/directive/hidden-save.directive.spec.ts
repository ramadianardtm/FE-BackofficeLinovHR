import { HiddenSaveDirective } from './hidden-save.directive';

describe('HiddenSaveDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenSaveDirective();
    expect(directive).toBeTruthy();
  });
});
